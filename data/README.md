# data

This folder is reserved for local datasets.

Expected local contents:

- `guage_read_coco/` - Roboflow COCO Object Detection export.
- `gauge_rtdetr_detection/` - prepared Ultralytics dataset generated by `prepare`.

Dataset files are intentionally not committed to Git because they are large. Use the README instructions to import or prepare the dataset locally.
